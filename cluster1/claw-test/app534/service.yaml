apiVersion: v1
kind: Service
metadata:
  name: perf-test4-app534-svc
  namespace: clawtest
  labels:
    app: perf-test4-app534
spec:
  selector:
    app: perf-test4-app534
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
  type: ClusterIP
